global:
  resolve_timeout: 1m

route:
  receiver: "discord"
  group_by: ["alertname"]
  group_wait: 10s
  group_interval: 30s
  repeat_interval: 5m

receivers:
  - name: "discord"
    webhook_configs:
      - url: "http://discord-webhook:9094"
        send_resolved: true
